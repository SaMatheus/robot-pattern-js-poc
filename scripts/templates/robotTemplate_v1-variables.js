import { screen } from '@testing-library/react';
import { expect } from '@jest/globals';
import setup from '../../../scripts/templates/utils/setup.js';

export const __ROBOT_NAME_WITH_VARIABLES__ = ({
    arrangeProps,
    actionProps,
    assertProps
  }) => {
  const arrange = { };

  const action = { };
  
  const assert = { };

  return {
    arrange,
    action,
    assert
  }
};

export default __ROBOT_NAME_WITH_VARIABLES__;

